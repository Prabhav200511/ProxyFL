# MIRACL Core provides NIST256 point arithmetic.  The optional native bridge
# provides SHA-256/AES-GCM; cryptography is the portable fallback when the
# platform-specific bridge has not been built.
cryptography>=42.0.0
