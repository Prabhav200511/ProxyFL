# Runtime dependencies for the ProxyFL VANET simulation.
#
# prettytable was previously undeclared: logger.py imports it at module scope,
# so a clean checkout failed to import main.py, server.py, device.py and the
# whole test suite with ModuleNotFoundError.
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
prettytable>=3.9.0

# Certificateless authentication layer (see also requirements-security.txt).
cryptography>=42.0.0

# Test suite.
pytest>=8.0.0
